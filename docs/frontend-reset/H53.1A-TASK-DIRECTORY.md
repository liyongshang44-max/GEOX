# H53.1a — Operator Evidence Twin Sensing Readback Display Cleanup

## Task directory

### A. Frontend display cleanup

- Replace the wide mixed sensing table with human-readable sensing cards.
- Show Soil Moisture as its own card.
- Show Sensing Window as its own card.
- Show Weather Forecast Input as its own card.
- Keep the page read-only.

### B. Pending node grouping

Group downstream nodes by generation stage:

1. Waiting for system derivation
   - WaterStressState
   - Forecast
   - Scenario
   - Recommendation

2. Waiting for operator/control flow
   - Approval
   - OperationPlan
   - AO-ACT
   - AsExecuted
   - Evidence

3. Waiting for closure and memory
   - Acceptance
   - Verification
   - ROI
   - Field Memory

### C. Boundary lock

This task must not add:

- POST / PUT / PATCH / DELETE from the Evidence Twin page
- approval creation
- operation plan creation
- AO-ACT task creation
- receipt submission
- acceptance writing
- ROI writing
- Field Memory writing

### D. Acceptance

Use the existing frontend acceptance file:

```powershell
node scripts/frontend_acceptance/ACCEPTANCE_H53_EVIDENCE_TWIN_SENSING_READBACK_V1.cjs
```

This acceptance now also checks H53.1a display requirements:

- `h53-sensing-soil-moisture`
- `h53-sensing-window`
- `h53-weather-forecast`
- `h53-pending-stage-groups`
- stage groups for system derivation, operator control flow, and closure/memory.
