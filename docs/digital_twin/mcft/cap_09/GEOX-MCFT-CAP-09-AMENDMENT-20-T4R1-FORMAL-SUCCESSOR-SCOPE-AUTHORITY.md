# GEOX MCFT-CAP-09 Amendment-20 — T4R1 Formal Successor Scope Authority

Status: CANDIDATE — EFFECTIVE ONLY WHEN MERGED TO PROTECTED `main`

Exact predecessor protected main: `dc916cd636777bdeb5413ff1245e4bec9ab465cb`

## 1. Purpose

This amendment authorizes a successor Formal scope candidate for MCFT-CAP-09 at KBS MCSE T4R1. It preserves the historical T1R1 and T3R1 authorities and does not itself activate runtime, write either v4 database or R2, arm EA5E2, or start O00–O23.

The successor exists because the current T3R1 crop-stage authority cannot conservatively keep one stage across a complete 24-slot Formal window, while the predeclared Alternative Scope Rescue selected T4R1 without outcome-driven rule changes. The merged T4R1 lifecycle and geometry qualifications support a fresh, independently bound scope whose current frozen stage is MID and whose governed Kc is 1.15 over a bounded legal window.

## 2. Historical authority preservation

These protected-main authorities remain immutable historical predecessors:

- `GEOX-MCFT-CAP-09-S6-FORMAL-SITE-AUTHORITY-V2.json` — T3R1;
- `GEOX-MCFT-CAP-09-S6-FORMAL-REALITY-BINDING-V2.json` — T3R1;
- `GEOX-MCFT-CAP-09-S6-FORMAL-CROP-CONTEXT-AUTHORITY-V2.json` — T3R1.

No T1R1 or T3R1 fact, canonical state, forecast, runtime config, database row, evidence artifact, or provider observation may be relabelled as T4R1.

## 3. Successor scope

- site: `KBS_MCSE_T4R1`;
- provider treatment: `T4` / Biologically Based;
- replicate: `R1`;
- field: `field_kbs_mcse_t4r1`;
- season: `season_2026_corn`;
- zone: `zone_kbs_mcse_t4r1_crop_formal_v1`;
- crop: corn;
- hybrid: Blue River `43-96P`;
- planting observation: KBS AgLog `6974`;
- planting local date: `2026-05-27` in `America/Detroit`;
- crop-only geometry semantic hash: `sha256:8108f8e38dbe1326cc3d10d05a96843d77bdf1fdb28b7ed28cb53126a6818868`.

The whole T4R1 plot is not crop-only. Its central prairie strip remains excluded. The successor zone is the independently qualified conservative crop-only polygon, not the provider whole-plot polygon.

## 4. Independent authority axes

Amendment-13 separation remains controlling.

### 4.1 Management lifecycle

The T4R1 lifecycle is consumed from the protected-main Amendment-16 persistent-state qualification. ACTIVE means governed management-season lifecycle, not a fresh biological observation. Provider silence is not evidence; retrieval time is not a provider coverage watermark.

### 4.2 Crop water-use stage

Stage is re-derived from all six frozen FAO56 maize-grain duration variants, the full local-calendar-day planting uncertainty, `T-6h` backward stability, and `T+30h` forward guard. No regional best-fit variant is selected.

### 4.3 Kc

Kc is resolved only after one unique stage exists across every variant and planting-time boundary. The value must be read from `mcft_crop_water_use_corn_v1` in the frozen MCFT-00 configuration matrix. Lifecycle, satellite greenness, unmapped phenology, or provider silence cannot create Kc.

## 5. Reused source authority

The Formal source binding matrix remains reusable only at its declared support classes: KBS weather is near-site meteorological support, Endpoint 25 soil is near-site point support, and GFS is model-grid support. No source is promoted to direct T4R1 field truth. A later runtime/source rebind must preserve these classifications and atomically pin the T4R1 scope.

## 6. Fresh bootstrap rule

T4R1 activation requires a fresh bootstrap. Cross-scope canonical stitching is forbidden.

No T1R1 or T3R1 canonical/runtime state may seed the T4R1 Formal run. The two v4 databases remain eligible only while zero-state; this amendment does not write them or authorize producer-bound rehydration.

## 7. Two-step activation boundary

Merging this amendment and the V3 authority documents establishes only a successor authority candidate. The active runtime remains T3R1 until a separate exact-main runtime/source-rebind change atomically updates consumed scope and authority pins and passes its focused gate.

After this amendment alone:

- `formal_successor_authority = QUALIFIED_CANDIDATE` may be true;
- `active_runtime_scope = T4R1` remains false;
- EA5E2 operational activation remains false;
- database, R2, runtime, scheduler, and Formal Evidence writes remain unauthorized;
- Formal execution remains `0/24`.

## 8. Fail-closed conditions

Runtime rebind is forbidden if any of these is unresolved or contradictory at the exact target boundary:

- lifecycle is not ACTIVE/RESOLVED/VALID;
- geometry hash differs from `sha256:8108f8e38dbe1326cc3d10d05a96843d77bdf1fdb28b7ed28cb53126a6818868`;
- planting identity differs from observation 6974 / 2026-05-27 / 43-96P / R1;
- stage is not unique across the full guard interval;
- Kc does not resolve uniquely from the frozen matrix;
- a future observation is required to pass;
- fewer than 24 contiguous legal target hours remain.

## 9. Nonclaims

No field calibration, direct field-soil truth, root-zone observation truth, model-grid observation truth, observed biological-stage truth, satellite lifecycle truth, cross-scope migration, v4 write, Formal start, or MCFT-CAP-09 completion is claimed.
