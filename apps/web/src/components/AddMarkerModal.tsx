// ⚠️ DEPRECATED: legacy only, do not use in new flows
import React from "react";

export default function AddMarkerModal(): React.ReactElement {
  return (
    <div className="card" style={{ padding: 12 }}>
      Legacy marker modal is deprecated and disabled for new flows.
    </div>
  );
}
