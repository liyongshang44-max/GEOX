export default function AdminBoundaryNotice(): React.ReactElement {
  return (
    <div className="adminBoundaryNotice">
      Admin Control Plane is read-only governance: no recommendation authoring, approval bypass, task creation, execution dispatch, ROI ledger, or Field Memory writes.
    </div>
  );
}
