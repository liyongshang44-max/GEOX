{
  "name": "@geox/telemetry-ingest",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx src/main.ts",
    "once": "tsx src/main.ts --once",
    "build": "tsc -p tsconfig.json && node scripts/prepare_runtime_workspace_links.cjs",
    "start": "node dist/apps/telemetry-ingest/src/main.js",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@geox/contracts": "workspace:*",
    "mqtt": "^5",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^5.4.5",
    "@types/node": "^20"
  }
}
