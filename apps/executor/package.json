{
  "name": "@geox/executor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "once": "tsx src/run_once.ts",
    "dispatch-once": "tsx src/run_dispatch_once.ts",
    "build": "node -e \"process.exit(0)\"",
    "dispatch-mqtt-once": "tsx src/run_mqtt_downlink_once.ts",
    "device-sim-once": "tsx src/run_mqtt_device_sim_once.ts",
    "receipt-uplink-once": "tsx src/run_mqtt_receipt_uplink_once.ts"
  },
  "dependencies": {
    "@geox/contracts": "workspace:*",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^5.4.5",
    "@types/node": "^20"
  }
}