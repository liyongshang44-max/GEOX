{
  "name": "@geox/executor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "runtime": "node dist/runtime_loop.js",
    "once": "node dist/run_once.js",
    "dispatch-once": "node dist/run_dispatch_once.js",
    "dispatch-mqtt-once": "node dist/run_mqtt_downlink_once.js",
    "device-sim-once": "node dist/run_mqtt_device_sim_once.js",
    "receipt-uplink-once": "node dist/run_mqtt_receipt_uplink_once.js",
    "dev:runtime": "tsx src/runtime_loop.ts",
    "dev:once": "tsx src/run_once.ts",
    "dev:dispatch-once": "tsx src/run_dispatch_once.ts",
    "dev:dispatch-mqtt-once": "tsx src/run_mqtt_downlink_once.ts",
    "dev:device-sim-once": "tsx src/run_mqtt_device_sim_once.ts",
    "dev:receipt-uplink-once": "tsx src/run_mqtt_receipt_uplink_once.ts"
  },
  "dependencies": {
    "@geox/contracts": "workspace:*",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^5.4.5",
    "@types/node": "^20"
  }
}
