// Controlplane dispatch-queue service façade placeholder.
// Runtime dispatch queue logic is currently hosted in task_service.ts during progressive extraction.
export function registerControlplaneDispatchQueueService(): void {
  // no-op: extracted incrementally.
}
