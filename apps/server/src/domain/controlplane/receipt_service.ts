// Controlplane receipt service façade placeholder.
// Receipt path logic is currently hosted in task_service.ts during progressive extraction.
export function registerControlplaneReceiptService(): void {
  // no-op: extracted incrementally.
}
