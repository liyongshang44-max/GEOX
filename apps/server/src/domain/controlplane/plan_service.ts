// Controlplane plan service façade placeholder.
// Operation plan transition logic is currently hosted in task_service.ts during progressive extraction.
export function registerControlplanePlanService(): void {
  // no-op: extracted incrementally.
}
