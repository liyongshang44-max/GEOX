name: b04c-stage1-physical-qc-guard-oneoff
on:
  push:
    branches:
      - docs/bline-b02-semantic-ownership-register-v1-test
permissions:
  contents: read
jobs:
  exact-head:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: c5b4c82af869751d13a96412f0d27522866ee8ad
          fetch-depth: 0
      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
      - run: pnpm install --no-frozen-lockfile
      - name: Server typecheck
        run: pnpm --filter @geox/server run typecheck
      - name: B04c pure guard fixtures
        run: pnpm --filter @geox/server exec node --import tsx --test src/evidence/stage1_physical_qc_consumption_guard_v1.test.ts
      - name: B04c Stage-1 loader fixtures
        run: pnpm --filter @geox/server exec node --import tsx --test src/services/device_observation_stage1_physical_qc_guard_v1.test.ts
      - name: B04a fixtures
        run: pnpm --filter @geox/server exec node --import tsx --test src/evidence/physical_qc_v1.test.ts
      - name: B04b1 fixtures
        run: pnpm --filter @geox/server exec node --import tsx --test src/evidence/ingress_physical_qc_snapshot_v1.test.ts
      - name: B04b1 observation persistence fixtures
        run: pnpm --filter @geox/server exec node --import tsx --test src/services/device_observation_ingress_physical_qc_v1.test.ts
      - name: B04b2 durable raw fixtures
        run: pnpm --filter @geox/server exec node --import tsx --test src/services/telemetry_ingest_durable_raw_v1.test.ts
      - name: Build telemetry-ingest compile prerequisites
        run: pnpm --filter @geox/contracts build && pnpm --filter @geox/device-skills build && pnpm --filter @geox/skill-registry build
      - name: Telemetry ingest package build
        run: pnpm --filter @geox/telemetry-ingest build
      - name: B04b3 MQTT source-preserving fixtures
        run: pnpm --filter @geox/telemetry-ingest exec node --import tsx --test src/mqtt_observation_input_v1.test.ts
      - name: B04b4 MQTT durable raw fixtures
        run: pnpm --filter @geox/telemetry-ingest exec node --import tsx --test src/mqtt_durable_raw_v1.test.ts
      - name: B02 linter
        run: node scripts/governance_acceptance/ACCEPTANCE_B02_SEMANTIC_CONTRACT_LINTER_V1.cjs
      - name: Marker
        run: |
          echo "B04C_EXACT_HEAD $(git rev-parse HEAD)"
          echo "B04C_STAGE1_PHYSICAL_QC_GUARD_PASS"
