name: mcft-cap-09-v12-v4-successor-one-shot-edit

on:
  pull_request:
    paths:
      - '.github/workflows/mcft-cap-09-v12-v4-successor-one-shot-edit.yml'

permissions:
  contents: write

jobs:
  exact-branch-edit:
    if: >-
      github.event.pull_request.head.repo.full_name == github.repository &&
      github.event.pull_request.head.ref == 'fix/mcft-cap09-t4r1-v12-formal-v4-successor' &&
      github.event.pull_request.base.ref == 'main'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0
          persist-credentials: true
      - name: Require exact PR identity and protected-main base
        shell: bash
        run: |
          set -euo pipefail
          test '${{ github.event.pull_request.number }}' = '3285'
          test '${{ github.event.pull_request.base.sha }}' = '274ce26d4f67049b891e253e148ed9be571c4bce'
          test "$(git rev-parse HEAD)" = '${{ github.event.pull_request.head.sha }}'
      - name: Apply exact v3-to-v4 operational token edits
        shell: python
        run: |
          from pathlib import Path

          old_db = 'geox_mcft_cap09_s6_formal_t4r1_24h_v3'
          new_db = 'geox_mcft_cap09_s6_formal_t4r1_24h_v4'
          old_failed = 'geox_mcft_cap09_s6_formal_t4r1_24h_v2'
          failed_archive = 'geox_mcft_cap09_s6_formal_t4r1_24h_v3_failed_o01_32660018684'

          exact_db_files = [
              'scripts/governance_acceptance/ASSEMBLE_MCFT_CAP_09_AMENDMENT_19_FORMAL_ARM_V1.cjs',
              'scripts/governance_acceptance/ASSEMBLE_MCFT_CAP_09_AMENDMENT_19_FORMAL_COMPLETION_V1.cjs',
              'scripts/runtime_acceptance/VERIFY_MCFT_CAP_09_AMENDMENT_19_FORMAL_DOWNSTREAM_ZERO_V1.ts',
              'scripts/runtime_acceptance/READBACK_MCFT_CAP_09_AMENDMENT_19_FORMAL_FINAL_V1.ts',
              '.github/workflows/mcft-cap-09-amendment19-formal-arm.yml',
              '.github/workflows/mcft-cap-09-amendment19-formal-a0-bootstrap.yml',
              '.github/workflows/mcft-cap-09-amendment19-formal-hourly-evidence.yml',
              '.github/workflows/mcft-cap-09-amendment19-formal-live-runner.yml',
              '.github/workflows/mcft-cap-09-amendment19-formal-final-readback.yml',
          ]
          counts = {}
          for name in exact_db_files:
              p = Path(name)
              s = p.read_text()
              n = s.count(old_db)
              if n < 1:
                  raise SystemExit(f'MCFT_CAP09_V12_V4_EXPECTED_DB_TOKEN_MISSING:{name}')
              s = s.replace(old_db, new_db)
              p.write_text(s)
              counts[name] = n

          arm = Path('scripts/governance_acceptance/ASSEMBLE_MCFT_CAP_09_AMENDMENT_19_FORMAL_ARM_V1.cjs')
          s = arm.read_text()
          marker = f'const FAILED_FORMAL_DATABASE = "{old_failed}";'
          if s.count(marker) != 1:
              raise SystemExit('MCFT_CAP09_V12_V4_ARM_FAILED_PREDECESSOR_TOKEN_CARDINALITY')
          arm.write_text(s.replace(marker, f'const FAILED_FORMAL_DATABASE = "{failed_archive}";'))

          print(counts)
      - name: Require diff is limited to declared operational bindings
        shell: bash
        run: |
          set -euo pipefail
          allowed='^(.github/workflows/mcft-cap-09-amendment19-formal-(arm|a0-bootstrap|hourly-evidence|live-runner|final-readback)\.yml|scripts/governance_acceptance/ASSEMBLE_MCFT_CAP_09_AMENDMENT_19_FORMAL_(ARM|COMPLETION)_V1\.cjs|scripts/runtime_acceptance/(VERIFY_MCFT_CAP_09_AMENDMENT_19_FORMAL_DOWNSTREAM_ZERO_V1\.ts|READBACK_MCFT_CAP_09_AMENDMENT_19_FORMAL_FINAL_V1\.ts))$'
          git diff --name-only | while read -r p; do [[ "$p" =~ $allowed ]] || { echo "MCFT_CAP09_V12_V4_UNDECLARED_EDIT:$p" >&2; exit 1; }; done
          test -n "$(git diff --name-only)"
      - name: Commit exact operational token edits
        shell: bash
        env:
          BRANCH: fix/mcft-cap09-t4r1-v12-formal-v4-successor
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add \
            scripts/governance_acceptance/ASSEMBLE_MCFT_CAP_09_AMENDMENT_19_FORMAL_ARM_V1.cjs \
            scripts/governance_acceptance/ASSEMBLE_MCFT_CAP_09_AMENDMENT_19_FORMAL_COMPLETION_V1.cjs \
            scripts/runtime_acceptance/VERIFY_MCFT_CAP_09_AMENDMENT_19_FORMAL_DOWNSTREAM_ZERO_V1.ts \
            scripts/runtime_acceptance/READBACK_MCFT_CAP_09_AMENDMENT_19_FORMAL_FINAL_V1.ts \
            .github/workflows/mcft-cap-09-amendment19-formal-arm.yml \
            .github/workflows/mcft-cap-09-amendment19-formal-a0-bootstrap.yml \
            .github/workflows/mcft-cap-09-amendment19-formal-hourly-evidence.yml \
            .github/workflows/mcft-cap-09-amendment19-formal-live-runner.yml \
            .github/workflows/mcft-cap-09-amendment19-formal-final-readback.yml
          git commit -m 'fix(mcft-cap09): bind active Formal chain to v4 store'
          git push origin "HEAD:$BRANCH"
