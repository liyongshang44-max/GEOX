name: MCFT-CAP-04 S5A postmerge probe

on:
  pull_request:
    types: [opened, reopened, synchronize]
    branches: [main]

permissions:
  contents: read

jobs:
  verify:
    if: github.event.pull_request.head.repo.full_name == github.repository && github.head_ref == 'agent/mcft-cap-04-s5a-postmerge-probe-v1'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout merged main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Verify exact merged-main identity
        run: |
          test "$(git branch --show-current)" = "main"
          test "$(git rev-parse HEAD)" = "2c6a0834488f367eb927430a15c9590c1bf348a3"
          test "$(git rev-parse origin/main)" = "2c6a0834488f367eb927430a15c9590c1bf348a3"

      - name: Run merged-main S5A governance Gate
        run: node scripts/governance_acceptance/ACCEPTANCE_MCFT_CAP_04_S5A_A1_A2_BUILDERS.cjs --postmerge

      - name: Verify complete task integrity
        run: |
          echo 'ea63e92a64b760b84c49428b1d3a245ce5cd94bb08daa9c6b971a53861b90a63  docs/digital_twin/mcft/cap_04/GEOX-MCFT-CAP-04-TASK.md' | sha256sum -c -
          test "$(wc -c < docs/digital_twin/mcft/cap_04/GEOX-MCFT-CAP-04-TASK.md)" -eq 77603
