#!/usr/bin/env node
'use strict';const fs=require('fs'),cp=require('child_process'),path=require('path'),assert=require('assert/strict');const R=path.resolve(__dirname,'../..');const m=JSON.parse(fs.readFileSync(path.join(R,'docs/digital_twin/mcft/cap_08/GEOX-MCFT-CAP-08-CHANGED-FILE-BOUNDARY-V1.json'),'utf8'));const base=process.env.MCFT_BASE_SHA||'ade35875ff6f5ef92ec76f04ab9fc302c57f700e';const actual=cp.execFileSync('git',['diff','--name-only',`${base}...HEAD`],{cwd:R,encoding:'utf8'}).trim().split(/\r?\n/).filter(Boolean).sort();assert.deepEqual(actual,m.changed_files);for(const p of actual){assert.ok(!p.startsWith('apps/web/src/'));assert.ok(!p.startsWith('apps/server/src/domain/'));assert.ok(!p.startsWith('apps/server/src/runtime/'));assert.ok(!p.startsWith('apps/server/db/migrations/'));}const r={status:'PASS',base_sha:base,changed_file_count:actual.length,changed_files:actual};fs.mkdirSync(path.join(R,'acceptance-output'),{recursive:true});fs.writeFileSync(path.join(R,'acceptance-output/MCFT_CAP_08_CHANGED_FILE_BOUNDARY_RESULT.json'),JSON.stringify(r,null,2)+'\n');console.log(JSON.stringify(r));
