#!/usr/bin/env node
// scripts/governance_acceptance/mcft_attestation_retention_store_v1.cjs
// Purpose: upload and read back MCFT exact-SHA attestation bytes through the frozen S3-compatible Object Lock authority.
// Boundary: workflow-only delivery support; never imported by Runtime server and never uses product Evidence Export credentials.
'use strict';
const assert=require('node:assert/strict');const crypto=require('node:crypto');const fs=require('node:fs');const os=require('node:os');const path=require('node:path');const cp=require('node:child_process');
const ROOT=path.resolve(__dirname,'../..'); const CONTRACT=path.join(ROOT,'docs/digital_twin/mcft/cap_07/GEOX-MCFT-CAP-07-ATTESTATION-RETENTION-STORE-CONTRACT-V1.json');
const sha=(b)=>`sha256:${crypto.createHash('sha256').update(b).digest('hex')}`; const load=(p)=>JSON.parse(fs.readFileSync(p,'utf8')); const safe=(s)=>{const v=String(s||'');if(!/^[A-Za-z0-9._-]+$/.test(v))throw new Error(`UNSAFE_SEGMENT:${v}`);return v;};
function canonical(v){if(Array.isArray(v))return `[${v.map(canonical).join(',')}]`;if(v&&typeof v==='object')return `{${Object.keys(v).sort().map(k=>`${JSON.stringify(k)}:${canonical(v[k])}`).join(',')}}`;return JSON.stringify(v);}
function aws(args,allowFail=false){const endpoint=process.env.GEOX_MCFT_ATTESTATION_S3_ENDPOINT;const region=process.env.GEOX_MCFT_ATTESTATION_S3_REGION;const full=['--region',region,...args];if(endpoint)full.unshift('--endpoint-url',endpoint);const r=cp.spawnSync('aws',full,{encoding:'utf8',env:process.env});if(r.status!==0&&!allowFail)throw new Error(`AWS_FAILED:${args.join(' ')}:${r.stderr.slice(0,500)}`);return r;}
function staticContract(){const c=load(CONTRACT);assert.equal(c.store_contract_id,'MCFT_ATTESTATION_S3_COMPAT_OBJECT_LOCK_V1');assert.equal(c.namespace_prefix,'mcft-attestations-v1');assert.equal(c.product_namespace_forbidden,'evidence-exports-v1');assert.equal(c.required_bucket_controls.versioning,'Enabled');assert.equal(c.required_bucket_controls.object_lock,'Enabled');assert.equal(c.required_bucket_controls.worm_mode,'COMPLIANCE');return c;}
function requireEnv(){for(const n of ['GEOX_MCFT_ATTESTATION_S3_ENDPOINT','GEOX_MCFT_ATTESTATION_S3_BUCKET','GEOX_MCFT_ATTESTATION_S3_REGION','AWS_ACCESS_KEY_ID','AWS_SECRET_ACCESS_KEY'])if(!process.env[n])throw new Error(`ENV_REQUIRED:${n}`);}
function preflight(){staticContract();requireEnv();const b=process.env.GEOX_MCFT_ATTESTATION_S3_BUCKET;const ver=JSON.parse(aws(['s3api','get-bucket-versioning','--bucket',b]).stdout||'{}');if(ver.Status!=='Enabled')throw new Error('BUCKET_VERSIONING_NOT_ENABLED');const lock=JSON.parse(aws(['s3api','get-object-lock-configuration','--bucket',b]).stdout||'{}');if(lock.ObjectLockConfiguration?.ObjectLockEnabled!=='Enabled')throw new Error('BUCKET_OBJECT_LOCK_NOT_ENABLED');return {versioning:ver.Status,object_lock:lock.ObjectLockConfiguration.ObjectLockEnabled};}
function put(bucket,key,file,retainUntil,contentType){const out=JSON.parse(aws(['s3api','put-object','--bucket',bucket,'--key',key,'--body',file,'--content-type',contentType,'--object-lock-mode','COMPLIANCE','--object-lock-retain-until-date',retainUntil]).stdout||'{}');if(!out.VersionId)throw new Error(`VERSION_ID_REQUIRED:${key}`);return out;}
function head(bucket,key,version){return JSON.parse(aws(['s3api','head-object','--bucket',bucket,'--key',key,'--version-id',version]).stdout||'{}');}
function get(bucket,key,version,file){aws(['s3api','get-object','--bucket',bucket,'--key',key,'--version-id',version,file]);}
function upload(){const contract=staticContract();const pf=preflight();const artifactPath=path.resolve(process.env.MCFT_CANONICAL_ARTIFACT_PATH||'');const archivePath=path.resolve(process.env.MCFT_ARTIFACT_ARCHIVE_PATH||'');if(!fs.existsSync(artifactPath)||!fs.existsSync(archivePath))throw new Error('ARTIFACT_INPUT_MISSING');const artifact=load(artifactPath);const semantic={...artifact};delete semantic.semantic_artifact_digest;const semanticDigest=sha(Buffer.from(canonical(semantic)));if(artifact.semantic_artifact_digest!==semanticDigest)throw new Error('SEMANTIC_ARTIFACT_DIGEST_MISMATCH');const archiveBytes=fs.readFileSync(archivePath);const transportDigest=sha(archiveBytes);const owner=safe(process.env.MCFT_REPOSITORY_OWNER);const repo=safe(process.env.MCFT_REPOSITORY_NAME);const cap=safe(artifact.capability_line_id);const slice=safe(artifact.slice_id.replace(/[^A-Za-z0-9._-]/g,'_'));const subject=safe(artifact.subject_commit);const run=safe(process.env.GITHUB_RUN_ID);const name=safe(process.env.MCFT_ARTIFACT_NAME);const prefix=`${contract.namespace_prefix}/${owner}/${repo}/${cap}/${slice}/${subject}/${run}/${name}`;if(prefix.includes('evidence-exports-v1'))throw new Error('PRODUCT_NAMESPACE_FORBIDDEN');const bucket=process.env.GEOX_MCFT_ATTESTATION_S3_BUCKET;const days=Number(process.env.MCFT_RETENTION_DAYS);if(!Number.isInteger(days)||days<180)throw new Error('RETENTION_DAYS_INVALID');const retainUntil=new Date(Date.now()+days*86400000).toISOString();const tmp=fs.mkdtempSync(path.join(os.tmpdir(),'mcft-retention-'));const transport={schema_version:'mcft_transport_metadata_v1',transport_archive_sha256:transportDigest,workflow_run_id:process.env.GITHUB_RUN_ID,job_id:process.env.GITHUB_JOB,uploaded_at:new Date().toISOString(),object_prefix:prefix};const retention={schema_version:'mcft_retention_manifest_v1',store_contract_id:contract.store_contract_id,retention_level:process.env.MCFT_RETENTION_LEVEL,retain_until:retainUntil,object_lock_mode:'COMPLIANCE',versioning_proof:pf.versioning,credential_identity_class:'geox_mcft_attestation_retention_writer_v1'};const transportPath=path.join(tmp,'transport-metadata.json');const retentionPath=path.join(tmp,'retention-manifest.json');fs.writeFileSync(transportPath,JSON.stringify(transport,null,2)+'\n');fs.writeFileSync(retentionPath,JSON.stringify(retention,null,2)+'\n');const files=[['canonical-artifact.json',artifactPath,'application/json'],['artifact-archive.bin',archivePath,'application/octet-stream'],['transport-metadata.json',transportPath,'application/json'],['retention-manifest.json',retentionPath,'application/json']];const versions={};for(const [n,f,ct] of files){const key=`${prefix}/${n}`;const existing=aws(['s3api','head-object','--bucket',bucket,'--key',key],true);if(existing.status===0)throw new Error(`IMMUTABLE_KEY_ALREADY_EXISTS:${key}`);const p=put(bucket,key,f,retainUntil,ct);versions[n]={key,version_id:p.VersionId,etag:p.ETag||null};const h=head(bucket,key,p.VersionId);if(h.ObjectLockMode!=='COMPLIANCE')throw new Error(`OBJECT_LOCK_MODE_INVALID:${key}`);if(!h.ObjectLockRetainUntilDate||Date.parse(h.ObjectLockRetainUntilDate)<Date.parse(retainUntil)-1000)throw new Error(`RETAIN_UNTIL_INVALID:${key}`);}
 const readDir=fs.mkdtempSync(path.join(os.tmpdir(),'mcft-readback-'));for(const [n] of files)get(bucket,versions[n].key,versions[n].version_id,path.join(readDir,n));const ra=load(path.join(readDir,'canonical-artifact.json'));const rs={...ra};delete rs.semantic_artifact_digest;if(sha(Buffer.from(canonical(rs)))!==semanticDigest)throw new Error('READBACK_SEMANTIC_DIGEST_MISMATCH');if(sha(fs.readFileSync(path.join(readDir,'artifact-archive.bin')))!==transportDigest)throw new Error('READBACK_TRANSPORT_DIGEST_MISMATCH');const denied=aws(['s3api','delete-object','--bucket',bucket,'--key',versions['artifact-archive.bin'].key,'--version-id',versions['artifact-archive.bin'].version_id],true);if(denied.status===0)throw new Error('LOCKED_VERSION_DELETE_UNEXPECTEDLY_SUCCEEDED');const locator={schema_version:'mcft_attestation_retention_locator_v1',store_contract_id:contract.store_contract_id,bucket,object_prefix:prefix,object_versions:versions,semantic_artifact_digest:semanticDigest,transport_archive_sha256:transportDigest,retention_level:process.env.MCFT_RETENTION_LEVEL,retain_until:retainUntil,readback_verified:true,locked_version_delete_denied:true};const out=path.join(ROOT,'acceptance-output/MCFT_CAP_07_ATTESTATION_RETENTION_LOCATOR.json');fs.mkdirSync(path.dirname(out),{recursive:true});fs.writeFileSync(out,JSON.stringify(locator,null,2)+'\n');console.log(JSON.stringify(locator));}
const mode=process.argv[2]||'--static-contract';try{if(mode==='--static-contract'){console.log(JSON.stringify(staticContract()));}else if(mode==='--preflight'){console.log(JSON.stringify(preflight()));}else if(mode==='--upload-readback'){upload();}else throw new Error(`MODE_INVALID:${mode}`);}catch(e){console.error(e);process.exit(1);}
