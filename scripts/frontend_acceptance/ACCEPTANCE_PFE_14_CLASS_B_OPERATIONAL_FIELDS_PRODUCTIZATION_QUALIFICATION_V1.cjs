const fs=require('node:fs');const assert=require('node:assert/strict');const json=(p)=>JSON.parse(fs.readFileSync(p,'utf8'));
const q=json('docs/frontend-productization/PFE-14-CLASS-B-OPERATIONAL-FIELDS-PRODUCTIZATION-QUALIFICATION-V1.json');const candidate=json('docs/frontend-productization/PFE-14-CLASS-B-OPERATIONAL-FIELDS-PRODUCTIZATION-CANDIDATE-V1.json');const authority=json('docs/frontend-productization/PFE-14-CURRENT-AUTHORITY.json');
const fields=['runtime_degradation_status','degradation_reason_codes','forecast_status','scenario_source_eligible','slot_window'];
assert.equal(q.record_status,'CLASS_B_OPERATIONAL_FIELDS_PRODUCTIZATION_QUALIFIED_S4_SLICE_BOUNDARY_READJUDICATION_AUTHORIZED_NOT_EFFECTIVE');assert.equal(q.qualified_subject_sha,'fd9a5c093c59d04ce5715e35952eb8dc71b99083');assert.equal(q.focused_run_id,31613116967);assert.equal(q.cap07_lifecycle_run_id,31613117071);assert.equal(q.standard_ci_run_id,31613117068);assert.equal(q.frontend_runtime_page_audit_pass,true);assert.equal(q.full_acceptance_pass,true);assert.equal(q.commercial_mvp0_release_gate_pass,true);assert.equal(q.all_pass,true);assert.equal(q.protected_main_merge_claimed,false);assert.deepEqual(q.qualified_server_fields_consumed,fields);assert.equal(q.browser_status_derivation_added,false);assert.equal(q.canonical_payload_parsing_added,false);assert.equal(q.not_materialized_failure_inference_added,false);assert.equal(q.runtime_mode_claimed,false);assert.equal(q.missed_backfill_restart_recovery_inferred,false);assert.equal(q.kbs_policy_effect,false);assert.equal(q.s4_effective,false);assert.equal(q.taskbook_amendment_authorized,false);assert.equal(q.next_action,'PFE_14_S4_SLICE_BOUNDARY_READJUDICATION');
assert.equal(candidate.record_status,'IMPLEMENTED_CANDIDATE_NOT_QUALIFIED_NOT_EFFECTIVE');assert.deepEqual(candidate.consumed_server_fields,fields);assert.equal(candidate.pfe14_s4_effective,false);
assert.equal(authority.record_status,'S4_NARROW_CLASS_B_OPERATIONAL_FIELDS_PRODUCTIZATION_QUALIFIED_SLICE_BOUNDARY_READJUDICATION_AUTHORIZED_NOT_EFFECTIVE');assert.equal(authority.class_b_operational_fields_productization_qualified,true);assert.equal(authority.s4_slice_boundary_readjudication_authorized,true);assert.equal(authority.class_b_operational_fields_productization_proof.subject_sha,q.qualified_subject_sha);assert.equal(authority.class_b_operational_fields_productization_proof.focused_run_id,q.focused_run_id);assert.equal(authority.class_b_operational_fields_productization_proof.cap07_lifecycle_run_id,q.cap07_lifecycle_run_id);assert.equal(authority.class_b_operational_fields_productization_proof.standard_ci_run_id,q.standard_ci_run_id);assert.equal(authority.class_b_operational_fields_productization_proof.all_pass,true);assert.equal(authority.class_b_operational_fields_productization_proof.merged_to_protected_main,false);assert.equal(authority.first_legal_next_action,q.next_action);assert.equal(authority.s4_effective,false);
console.log(JSON.stringify({status:'PASS',qualification:'PFE-14-CLASS-B-OPERATIONAL-FIELDS-PRODUCTIZATION-QUALIFICATION-V1',qualified_subject_sha:q.qualified_subject_sha,server_fields_consumed:fields,s4_slice_boundary_readjudication_authorized:true,pfe14_s4_effective:false,next_action:q.next_action},null,2));