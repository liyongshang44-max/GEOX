'use strict';
const assert=require('node:assert/strict');
const SURFACES=[{name:'runtime',paginated:false},{name:'timeline',paginated:true},{name:'trace',paginated:false},{name:'states',paginated:true},{name:'forecasts',paginated:true},{name:'scenarios',paginated:true},{name:'residuals',paginated:true},{name:'action-lifecycle',paginated:true},{name:'model-governance',paginated:true,variants:['CALIBRATION_CANDIDATE','SHADOW_EVALUATION']},{name:'health',paginated:false}];
async function fetchVariantV1(request,spec,surface,variant=null){const pages=[];let cursor=null;do{const response=await request({scope:spec.scope,surface:surface.name,collection_kind:variant,cursor,limit:10});assert.equal(response.status,200,`CAP07_STATUS:${surface.name}`);assert.equal(response.cache_control,'no-store');assert.match(response.content_hash,/^sha256:[0-9a-f]{64}$/);assert.match(response.response_hash,/^sha256:[0-9a-f]{64}$/);pages.push(response);cursor=surface.paginated?(response.next_cursor??null):null;assert.ok(pages.length<=1000,'CAP07_PAGINATION_LOOP');}while(cursor!==null);return pages;}
async function executeCompleteCap07ReadbackV1(port,spec,authority){assert.equal(authority?.record_status,'SINGLE_FINAL_FORMAL_RUN_DATABASE_EXECUTION_AUTHORIZED','DATABASE_EXECUTION_AUTHORITY_REQUIRED');const surfaces=[];for(const surface of SURFACES){const variants=surface.variants??[null];for(const variant of variants)surfaces.push({name:surface.name,variant,pages:await fetchVariantV1(port.request.bind(port),spec,surface,variant)});}return{schema_version:'geox_mcft_cap08_s6_cap07_complete_readback_result_v1',surface_definition_count:10,request_variant_count:11,surfaces,pagination_until_cursor_null:true,product_read_write_delta:0,canonical_fact_write_delta:0,projection_write_delta:0};}
module.exports={SURFACES,fetchVariantV1,executeCompleteCap07ReadbackV1};
